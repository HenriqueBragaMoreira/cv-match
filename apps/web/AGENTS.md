# CV Match — Web

Next.js frontend with shadcn/ui for the CV Match analysis interface.

## Docs

- `../../docs/REQUIREMENTS.md` — full requirement definitions + aggregated status
- `../../docs/BUSINESS-RULES.md` — full business rule definitions + aggregated status
- `../../docs/web/REQUIREMENTS.md` — Web implementation status per requirement
- `../../docs/web/BUSINESS-RULES.md` — Web-relevant rules + enforcement status

When implementing features or fixing bugs, update status in `../../docs/web/`. Promote root doc status only when all layers reach the same level.
