# CV Match — API

REST API on Hono + Cloudflare Workers for AI-powered ATS resume analysis.

## Docs

- `../../docs/REQUIREMENTS.md` — full requirement definitions + aggregated status
- `../../docs/BUSINESS-RULES.md` — full business rule definitions + aggregated status
- `../../docs/api/REQUIREMENTS.md` — API implementation status per requirement
- `../../docs/api/BUSINESS-RULES.md` — API-relevant rules + enforcement status

When implementing features or fixing bugs, update status in `../../docs/api/`. Promote root doc status only when all layers reach the same level.
