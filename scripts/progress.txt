[2026-04-08] Task 1.1 done — Installed Vercel AI SDK and providers (@ai-sdk/openai, @ai-sdk/anthropic, @ai-sdk/google, ai) in the API app. Type-check passes.
[2026-04-08] Task 1.2 done — Installed zod (^4.3.6) in the API app for request/response schema validation. Type-check passes.
[2026-04-08] Task 1.3 done — Created API folder structure: src/routes/, src/schemas/, src/prompts/, src/services/, src/utils/. Extracted health route to routes/health.ts and wired it via barrel export. Type-check and lint pass.
[2026-04-08] Task 1.4 done — Configured global error handling middleware in Hono. Created src/errors.ts with AppError class (validation, invalidApiKey, provider, notFound, internal), API key sanitization (OpenAI sk-/sk-proj-, Anthropic sk-ant-, Google AIza patterns), ZodError handling, HTTPException handling, and 404 not-found handler. Wired into app via app.onError() and app.notFound(). Type-check and lint pass.
[2026-04-08] Task 1.5 done — Configured CORS middleware in Hono using built-in hono/cors. Allows localhost:3000 (Next.js dev), permits GET/POST/OPTIONS methods, Content-Type header, with 24h preflight cache. Type-check and lint pass.
[2026-04-08] Task 2.1 done — Defined Zod schema for /analyze request (analyzeRequestSchema) in src/schemas/analyze.ts. Fields: resumeFile (File, .pdf/.tex, max 10 MB via z.instanceof(File) + refine), jobDescription (string, 1–50k chars), provider (enum: openai | anthropic | google), apiKey (string, required). Shared resumeFileSchema exported. Request via multipart/form-data. Exported via barrel index. Type-check and lint pass.
[2026-04-08] Task 2.2 done — Defined Zod schema for /analyze response (analyzeResponseSchema) in src/schemas/analyze.ts. Fields: score (int 0-100), strengths[] (min 1), weaknesses[] (min 1), suggestions[] (min 1), keywords { present[], missing[] }, breakdown { experience, skills, education, certifications } (each int 0-100), formattingWarnings[]. Exported via barrel index. Type-check and lint pass.
[2026-04-08] Task 2.3 done — Defined Zod schema for /improve request (improveRequestSchema) in src/schemas/improve.ts. Fields: resumeFile (File, .pdf/.tex, reuses resumeFileSchema), jobDescription (string, 1–50k chars), analysisResult (reuses analyzeResponseSchema), provider (enum: openai | anthropic | google), apiKey (string, required). Request via multipart/form-data. Exported via barrel index. Type-check and lint pass.
[2026-04-08] Task 2.4 done — Defined Zod schema for /improve response (improveResponseSchema) in src/schemas/improve.ts. Fields: improvedResume (string, min 1), changes (string, min 1), newScore (int 0-100). Exported via barrel index. Type-check and lint pass.
[2026-04-09] Task 3.1 done — Created ATS analysis system prompt in src/prompts/ats-analysis.ts. Exports ATS_ANALYSIS_SYSTEM_PROMPT (system prompt with strict evaluation rules, scoring guidelines, and JSON output spec matching analyzeResponseSchema) and buildAtsAnalysisUserPrompt() helper (formats resume text + job description for the user message). Barrel export added in src/prompts/index.ts. Enforces BR-003 (strict ATS evaluation, no inferred qualifications) and BR-004 (score range). Type-check and lint pass.
[2026-04-09] Task 3.2 done — Created CV improvement system prompt in src/prompts/cv-improvement.ts. Exports CV_IMPROVEMENT_SYSTEM_PROMPT (rewriting rules: no fabrication, address every weakness, optimize without inflating, preserve facts, ATS-friendly formatting, match original language) and buildCvImprovementUserPrompt() helper (formats original resume + job description + analysis result for the user message). Output spec matches improveResponseSchema (improvedResume, changes). Barrel export added in src/prompts/index.ts. Enforces BR-006 (no fabricated qualifications, original resume as source of truth). Type-check and lint pass.
