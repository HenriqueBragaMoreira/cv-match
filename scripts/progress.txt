[2026-04-08] Task 1.1 done — Installed Vercel AI SDK and providers (@ai-sdk/openai, @ai-sdk/anthropic, @ai-sdk/google, ai) in the API app. Type-check passes.
[2026-04-08] Task 1.2 done — Installed zod (^4.3.6) in the API app for request/response schema validation. Type-check passes.
[2026-04-08] Task 1.3 done — Created API folder structure: src/routes/, src/schemas/, src/prompts/, src/services/, src/utils/. Extracted health route to routes/health.ts and wired it via barrel export. Type-check and lint pass.
[2026-04-08] Task 1.4 done — Configured global error handling middleware in Hono. Created src/errors.ts with AppError class (validation, invalidApiKey, provider, notFound, internal), API key sanitization (OpenAI sk-/sk-proj-, Anthropic sk-ant-, Google AIza patterns), ZodError handling, HTTPException handling, and 404 not-found handler. Wired into app via app.onError() and app.notFound(). Type-check and lint pass.
[2026-04-08] Task 1.5 done — Configured CORS middleware in Hono using built-in hono/cors. Allows localhost:3000 (Next.js dev), permits GET/POST/OPTIONS methods, Content-Type header, with 24h preflight cache. Type-check and lint pass.
[2026-04-08] Task 2.1 done — Defined Zod schema for /analyze request (analyzeRequestSchema) in src/schemas/analyze.ts. Fields: resumeFile (File, .pdf/.tex, max 10 MB via z.instanceof(File) + refine), jobDescription (string, 1–50k chars), provider (enum: openai | anthropic | google), apiKey (string, required). Shared resumeFileSchema exported. Request via multipart/form-data. Exported via barrel index. Type-check and lint pass.
[2026-04-08] Task 2.2 done — Defined Zod schema for /analyze response (analyzeResponseSchema) in src/schemas/analyze.ts. Fields: score (int 0-100), strengths[] (min 1), weaknesses[] (min 1), suggestions[] (min 1), keywords { present[], missing[] }, breakdown { experience, skills, education, certifications } (each int 0-100), formattingWarnings[]. Exported via barrel index. Type-check and lint pass.
[2026-04-08] Task 2.3 done — Defined Zod schema for /improve request (improveRequestSchema) in src/schemas/improve.ts. Fields: resumeFile (File, .pdf/.tex, reuses resumeFileSchema), jobDescription (string, 1–50k chars), analysisResult (reuses analyzeResponseSchema), provider (enum: openai | anthropic | google), apiKey (string, required). Request via multipart/form-data. Exported via barrel index. Type-check and lint pass.
[2026-04-08] Task 2.4 done — Defined Zod schema for /improve response (improveResponseSchema) in src/schemas/improve.ts. Fields: improvedResume (string, min 1), changes (string, min 1), newScore (int 0-100). Exported via barrel index. Type-check and lint pass.
[2026-04-09] Task 3.1 done — Created ATS analysis system prompt in src/prompts/ats-analysis.ts. Exports ATS_ANALYSIS_SYSTEM_PROMPT (system prompt with strict evaluation rules, scoring guidelines, and JSON output spec matching analyzeResponseSchema) and buildAtsAnalysisUserPrompt() helper (formats resume text + job description for the user message). Barrel export added in src/prompts/index.ts. Enforces BR-003 (strict ATS evaluation, no inferred qualifications) and BR-004 (score range). Type-check and lint pass.
[2026-04-09] Task 3.2 done — Created CV improvement system prompt in src/prompts/cv-improvement.ts. Exports CV_IMPROVEMENT_SYSTEM_PROMPT (rewriting rules: no fabrication, address every weakness, optimize without inflating, preserve facts, ATS-friendly formatting, match original language) and buildCvImprovementUserPrompt() helper (formats original resume + job description + analysis result for the user message). Output spec matches improveResponseSchema (improvedResume, changes). Barrel export added in src/prompts/index.ts. Enforces BR-006 (no fabricated qualifications, original resume as source of truth). Type-check and lint pass.
[2026-04-09] Task 3.3 done — Created re-scoring prompt module in src/prompts/re-scoring.ts. Re-exports ATS_ANALYSIS_SYSTEM_PROMPT as RE_SCORING_SYSTEM_PROMPT and buildAtsAnalysisUserPrompt as buildReScoringUserPrompt, ensuring the /improve endpoint uses the exact same prompt and parameters as /analyze for re-scoring (BR-005). Barrel export added in src/prompts/index.ts. Updated docs/api/BUSINESS-RULES.md BR-005 status to implemented. Type-check and lint pass.
[2026-04-09] Task 4.1 done — Created AI provider factory in src/services/provider-factory.ts. Exports createProviderModel(provider, apiKey) that returns a configured model instance for openai (gpt-4o-mini), anthropic (claude-sonnet-4-20250514), or google (gemini-2.0-flash) using Vercel AI SDK provider factories. Barrel export added in src/services/index.ts. Type-check and lint pass.
[2026-04-09] Task 4.2 done — Implemented API key format validation per provider. Created src/utils/api-key-validation.ts with validateApiKeyFormat(provider, apiKey) that checks basic key prefix patterns: OpenAI (sk-), Anthropic (sk-ant-), Google (AIza). Integrated into analyzeRequestSchema and improveRequestSchema via Zod superRefine for cross-field validation. Barrel export added in src/utils/index.ts. Updated FR-003 status to implemented. Type-check and lint pass.
[2026-04-09] Task 6.2 done — Installed unpdf (^1.4.0) and created src/utils/pdf-parser.ts with extractTextFromPdf(data) that extracts text from PDF files using unpdf (PDF.js serverless build), compatible with Cloudflare Workers edge runtime. Throws on empty/image-only PDFs. Barrel export added in src/utils/index.ts. Type-check and lint pass.
[2026-04-09] Task 6.3 done — Created src/utils/latex-parser.ts with extractTextFromLatex(raw) that strips LaTeX commands to extract plain text. Handles: preamble removal, \begin/\end{document} boundaries, comments, \section/\textbf/\emph/\href/\url extraction, \item bullets, special characters (``,'',---,--,~), environment stripping, and catch-all command removal. Throws on empty content. Barrel export added in src/utils/index.ts. Tests (8 passing) in src/utils/latex-parser.test.ts. Type-check and lint pass. Phase 6 (Parsing) is now 4/4 complete.
[2026-04-09] Task 5.1 done — Implemented POST /analyze endpoint in src/routes/analyze.ts. Parses multipart/form-data, validates request with analyzeRequestSchema (Zod), extracts text from PDF (unpdf) or LaTeX files, initializes AI provider via factory, calls generateObject (Vercel AI SDK) with ATS analysis prompt and analyzeResponseSchema for structured output. Includes extractResumeText helper (maps extraction errors to AppError.validation) and toProviderError helper (maps 401/unauthorized to AppError.invalidApiKey, others to AppError.provider). Route mounted at /analyze in src/index.ts. Barrel export added in src/routes/index.ts. Type-check, lint, and tests (8 passing) all pass.
[2026-04-09] Task 5.2 done — Implemented POST /improve endpoint in src/routes/improve.ts. Parses multipart/form-data, validates request with improveRequestSchema (Zod), including JSON-parsing analysisResult from string. Extracts text from PDF or LaTeX files, initializes AI provider via factory. Two-step AI workflow: (1) generates improved CV using CV_IMPROVEMENT_SYSTEM_PROMPT with cvImprovementSchema for structured output (improvedResume + changes), (2) re-scores the improved CV using RE_SCORING_SYSTEM_PROMPT (same ATS prompt as /analyze, enforcing BR-005) with analyzeResponseSchema. Returns { improvedResume, changes, newScore }. Added cvImprovementSchema to src/schemas/improve.ts (improveResponseSchema now extends it). Route mounted at /improve in src/index.ts. Barrel export added in src/routes/index.ts. Updated FR-012, FR-013 to implemented, BR-006, BR-007 to implemented. Type-check, lint, and tests (8 passing) all pass.
[2026-04-09] Task 5.3 done — Health check GET / endpoint was already implemented in src/routes/health.ts (created during task 1.3) returning { status: "ok" }. Route mounted at / in src/index.ts. Marked as done — Phase 5 (Endpoints) is now 3/3 complete. Type-check and tests (8 passing) all pass.
[2026-04-09] Task 7.1 done — Created unit tests for all Zod schemas in src/schemas/analyze.test.ts (30 tests) and src/schemas/improve.test.ts (18 tests). Coverage: aiProvider enum validation, resumeFileSchema (size limits, extensions, empty files), analyzeRequestSchema (valid requests, API key format validation per provider, field constraints), analyzeResponseSchema (score range/integer, required arrays, breakdown scores, keywords), improveRequestSchema (nested analysisResult validation, API key cross-field validation), cvImprovementSchema (required fields), improveResponseSchema (newScore range/integer). All 56 tests pass. Type-check passes.
[2026-04-09] Task 7.2 done — Created unit tests for AI provider factory in src/services/provider-factory.test.ts (9 tests). Coverage: verifies each provider (openai, anthropic, google) is created with the correct API key, uses the correct default model (gpt-4o-mini, claude-sonnet-4-20250514, gemini-2.0-flash), and returns the model instance. Mocks @ai-sdk/openai, @ai-sdk/anthropic, @ai-sdk/google. All 65 tests pass. Type-check passes.
[2026-04-09] Task 7.3 done — Created unit tests for PDF parser in src/utils/pdf-parser.test.ts (8 tests). Coverage: valid text extraction from ArrayBuffer and Uint8Array inputs, whitespace trimming, empty PDF error, whitespace-only PDF error, error propagation from getDocumentProxy and extractText, mergePages option verification. Mocks unpdf via vi.hoisted. LaTeX parser tests (8 tests) already existed from task 6.3 in src/utils/latex-parser.test.ts. All 73 tests pass. Type-check passes.
[2026-04-09] Task 7.4 done — Created integration tests for POST /analyze endpoint in src/routes/analyze.test.ts (26 tests). Coverage: happy path (PDF and LaTeX files, all three providers, extracted text passed to AI model), response format validation (all required fields, score range 0-100, boundary scores), validation errors (missing fields, unsupported file extension, empty file, invalid provider, mismatched provider/key, VALIDATION_ERROR code), file extraction errors (PDF and LaTeX failures return 400), AI provider errors (401 for unauthorized/invalid key, 502 for generic errors, API key redaction in error messages), and routing (404 for GET /analyze). Mocks AI SDK generateObject and file parsers. All 99 tests pass. Type-check passes.
[2026-04-10] Task 7.5 done — Created integration tests for POST /improve endpoint in src/routes/improve.test.ts (33 tests). Coverage: happy path (PDF and LaTeX files, all three providers, two-step generateObject calls for improvement + re-scoring, extracted text and analysis passed to improvement call, improved resume passed to re-scoring call, re-scoring uses same ATS prompt as /analyze), response format validation (all required fields, newScore range 0-100, boundary scores, no extra re-scoring fields leaked), validation errors (missing fields, invalid/malformed analysisResult, unsupported file extension, empty file, invalid provider, mismatched provider/key, VALIDATION_ERROR code), file extraction errors (PDF and LaTeX failures return 400), AI provider errors (401 on improvement and re-scoring calls, 502 for generic/unknown errors, API key redaction), and routing (404 for GET /improve). Mocks AI SDK generateObject and file parsers. All 132 tests pass. Type-check passes. Phase 7 (API Tests) is now 5/5 complete.
[2026-04-10] Task 8.1 done — Installed shadcn/ui components in the web app: input, textarea, select, label, card, tabs, alert, badge, progress. These cover all UI needs for Phases 9–11 (form, results, improvement). Fixed Biome lint warnings (import type) in all generated component files. Button was already installed. Type-check and lint pass. API tests (132) still pass.
[2026-04-10] Task 8.2 done — Created base application layout in the web app. Root layout.tsx now includes: header with "CV Match" project name and bottom border, centered responsive main container (max-w-3xl with horizontal padding), footer with GitHub repository link. Set html lang to "pt-BR" and added metadata (title + description in pt-BR). Updated page.tsx with introductory heading and description in pt-BR. Body uses min-h-svh flex column for sticky footer. Type-check, lint, and API tests (132) all pass. Phase 8 (Setup do Frontend) is now 2/2 complete.
[2026-04-10] Task 9.1 done — Created ProviderSelect component in src/components/provider-select.tsx. Client component using shadcn/ui Select with Label. Lists three supported AI providers (OpenAI, Anthropic, Google) in a dropdown. Exports AiProvider type. Props: value (controlled) and onValueChange callback. UI text in pt-BR ("Provedor de IA", "Selecione um provedor"). Type-check and lint pass. API tests (132) still pass.
[2026-04-10] Task 9.2 done — Created ApiKeyInput component in src/components/api-key-input.tsx. Client component with password input field and visibility toggle (Eye/EyeOff icons via lucide-react). Shows provider-specific placeholder hints (sk-... for OpenAI, sk-ant-... for Anthropic, AIza... for Google). Input is disabled when no provider is selected. Uses shadcn/ui Input, Button (ghost icon-xs), and Label. Value stored in React state via controlled props (value + onChange). UI text in pt-BR ("Chave de API", "Ocultar chave"/"Mostrar chave"). Type-check, lint, and API tests (132) all pass.
[2026-04-10] Task 9.3 done — Created ResumeUpload component in src/components/resume-upload.tsx. Client component with drag-and-drop zone and click-to-select file picker. Accepts only .pdf and .tex files (max 10 MB). Client-side validation for file extension, size, and empty files with pt-BR error messages. Displays selected file name and formatted size with remove button (X icon). Drop zone highlights on drag-over. Uses semantic <button> element for accessibility. Controlled props: file (File | null) and onFileChange callback. Uses lucide-react icons (FileUp, X), shadcn/ui Button and Label. UI text in pt-BR ("Currículo", "Arraste e solte ou clique para selecionar", "Remover arquivo"). Type-check, lint, and API tests (132) all pass.
[2026-04-10] Task 9.4 done — Created JobDescriptionInput component in src/components/job-description-input.tsx. Client component with shadcn/ui Textarea and Label. Features: pt-BR placeholder ("Cole aqui a descrição da vaga..."), character counter displaying current/max (50,000) formatted with pt-BR locale, enforces max character limit on input. Controlled props: value (string) and onChange callback. Uses same space-y-2 layout pattern as other form components. Type-check, lint, and API tests (132) all pass.
[2026-04-10] Task 9.5 done — Created AnalysisForm component in src/components/analysis-form.tsx. Integrates all four form sub-components (ProviderSelect, ApiKeyInput, ResumeUpload, JobDescriptionInput) into a Card layout with a submit button. Features: client-side validation disables submit until all required fields are filled (provider, API key, file, job description), loading state with spinner icon during submission (Loader2 + "Analisando..."), two-column grid layout for provider/API key on sm+ screens. Updated page.tsx to render AnalysisForm as the main page content. API call will be wired in task 9.6. Type-check, lint, and API tests (132) all pass.
[2026-04-10] Task 9.6 done — Implemented API call from AnalysisForm to POST /analyze. Created src/services/api.ts with: AnalysisResult type (mirrors analyzeResponseSchema), ApiError class (code, statusCode, message), analyzeResume() function that sends multipart/form-data (resumeFile, jobDescription, provider, apiKey) to the API. Error handling covers: network failures (fetch throws), invalid API key (INVALID_API_KEY → 401), validation errors (VALIDATION_ERROR → 400), provider errors (PROVIDER_ERROR → 502), and unknown errors. User-facing error messages in pt-BR. API base URL configurable via NEXT_PUBLIC_API_URL env var (defaults to http://localhost:8787). Updated AnalysisForm: async handleSubmit calls analyzeResume(), displays errors in a destructive Alert with AlertCircle icon, clears errors on new submission, exposes optional onResult callback for parent components. Phase 9 (Formulário de Análise) is now 6/6 complete. Type-check, lint, and API tests (132) all pass.
[2026-04-10] Task 10.1 done — Created ScoreDisplay component in src/components/score-display.tsx. Displays score 0-100 with circular SVG progress ring and numeric value in center. Color-coded by range: 0-39 red ("Baixa compatibilidade"), 40-69 yellow ("Compatibilidade parcial"), 70-100 green ("Boa compatibilidade"). Uses animated stroke-dashoffset transition for visual feedback. Score clamped and rounded. Accessible with role="status" on label. Type-check, lint, and API tests (132) all pass.
[2026-04-10] Task 10.2 done — Created StrengthsWeaknesses component in src/components/strengths-weaknesses.tsx. Two-column grid layout (stacks on mobile) with Card per column. Strengths card uses green ThumbsUp icon and ✓ markers; weaknesses card uses red ThumbsDown icon and ✗ markers. Lists render each item with colored check/cross prefix and text. Props: strengths (string[]) and weaknesses (string[]). Uses shadcn/ui Card components and lucide-react icons. Dark mode supported. Build, lint, and API tests (132) all pass.
[2026-04-10] Task 10.3 done — Created SuggestionsList component in src/components/suggestions-list.tsx. Ordered list inside a Card with Lightbulb icon header. Each suggestion has a numbered circle indicator and a priority Badge (Alta for #1, Média for #2-3, Baixa for #4+) with color-coded styling (red/yellow/green). Props: suggestions (string[]). Uses shadcn/ui Card, Badge components and lucide-react Lightbulb icon. Dark mode supported. Build, type-check, lint, and API tests (132) all pass.
[2026-04-10] Task 10.4 done — Created KeywordsAnalysis component in src/components/keywords-analysis.tsx. Two-column grid layout (stacks on mobile) matching StrengthsWeaknesses pattern. Present keywords card uses green Tag icon header and green Badges with ✓ prefix; missing keywords card uses red Tag icon header and red Badges with ✗ prefix. Handles empty arrays with muted placeholder text. Props: present (string[]) and missing (string[]). Uses shadcn/ui Card, Badge components and lucide-react Tag icon. Dark mode supported. Type-check, lint, and API tests (132) all pass.
